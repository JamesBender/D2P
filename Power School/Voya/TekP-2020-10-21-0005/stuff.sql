select * from dbo.U_EVOYAEOIEX_drvTbl;

--select * from dbo.U_dsi_BDM_EVOYAEOIEX WITH (NOLOCK)

--sp_help U_dsi_BDM_EVOYAEOIEX


--select top 10 EedEEID , EedCOID, EedDedCode, EedNeedEOI,* from EmpDed

-- if emp but not spouse, send with out spouse
-- if spouse but not empl , only send emp demo (no policy even if they have it) and spouse data

--  'USESL (employee) ,USSL(spouse) ';
